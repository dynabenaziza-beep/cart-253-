

function resetVariation3() {
    // later i'll reset all my variables here
}

function variation3Draw() {
    // later this will be the main draw for variation 3
    background(200); // temp background so i see something
    fill(0);
    textAlign(CENTER, CENTER);
    textSize(24);
    text("variation 3 - garfield frog (work in progress)", width / 2, height / 2);
}

function variation3MousePressed() {
    // i'll use this later if i want to go back to menu or restart
}